# Open WebUI Configuration
# For WSL: Use localhost or 127.0.0.1 if Open WebUI is running in WSL
# For WSL accessing Windows services: Use the Windows host IP (often 172.x.x.1)
OPENWEBUI_URL=http://localhost:8080
OPENWEBUI_EMAIL=UWAT@sonalysts.com
OPENWEBUI_PASSWORD=password

# Presenton Configuration  
# For WSL: Use localhost or 127.0.0.1 if Presenton is running in WSL
# For WSL accessing Windows services: Use the Windows host IP (often 172.x.x.1)
PRESENTON_URL=http://localhost:5000

# Browser Configuration (optional)
HEADLESS=false
DEBUG=true

# Timeout Configuration (optional)
# Values in milliseconds - WSL may need longer timeouts
BROWSER_TIMEOUT=300000
BUTTON_WAIT_TIMEOUT=900000
RESPONSE_WAIT_TIMEOUT=180000

# WSL-specific notes:
# - Make sure DISPLAY is set if using GUI: export DISPLAY=:0
# - Consider using Windows host IP if services run on Windows
# - Increase timeouts if experiencing slower performance